## 🧠 CORE PYTHON / UTILITY
attrs==25.3.0               # Common low-level dependency for aiohttp etc.
click==8.2.1                # Flask CLI support
enum-tools==0.13.0          # Enum helpers (safe to keep)
itsdangerous==2.2.0         # Used by Flask session signing
MarkupSafe==3.0.2           # Jinja2 / Flask safety
python-dateutil==2.9.0.post0 # For date parsing/formatting (used in TwitchAPI, optional)
python-dotenv==1.1.1        # .env variable support (essential)
six==1.17.0                 # Legacy compat for dependencies
typing_extensions==4.14.1   # Type hinting support

## 🌐 WEB SERVER / FLASK STACK
Flask==3.1.1                # Core Flask framework
Flask-Session==0.8.0        # Server-side sessions
Flask-Talisman==1.1.0       # Security headers + HTTPS enforcement
gunicorn==21.2.0            # WSGI production web server
Werkzeug==3.1.3             # Flask internal routing engine
Jinja2==3.1.6               # Flask templating

## 🔌 ASYNC / HTTP CLIENTS (aiohttp stack)
aiohappyeyeballs==2.6.1     # DNS resolution improvements for aiohttp
aiohttp>=3.12               # Async HTTP library (required by discord.py)
aiosignal==1.4.0            # Internal aiohttp dependency
frozenlist==1.7.0           # Internal aiohttp dependency
idna==3.10                  # Domain name encoding (used by requests/aiohttp)
multidict==6.6.3            # aiohttp dependency for headers
propcache==0.3.2            # aiohttp cache helper
yarl==1.20.1                # URL handling for aiohttp

## 💬 DISCORD BOT
discord.py==2.5.2           # Discord bot framework (latest stable)
psycopg[binary]>=3.2        # PostgreSQL driver for database persistence

## 🎥 TWITCH INTEGRATION
twitchAPI==4.5.0            # Twitch REST + PubSub integration

## 🎞️ PLEX INTEGRATION
plex.py==0.9.0              # Plex control via websocket and API
PlexAPI==4.15.15            # Official Plex API wrapper

## 🌍 HTTP / REQUESTS
requests==2.32.3            # Sync HTTP client for REST APIs

## Redis / Caching
redis==5.0.1

## 🧰 MISC (OPTIONAL / NON-CRITICAL)
audioop-lts==0.2.1          # Only needed for Python builds missing audioop (safe to remove unless using voice)
blinker==1.9.0              # Signal library (used by Flask optional)
